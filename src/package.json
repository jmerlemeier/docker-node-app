{
  "name": "docker",
  "version": "1.0.0",
  "description": "Basic Node app with Docker",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "j.erle",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
